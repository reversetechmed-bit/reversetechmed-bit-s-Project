import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Textarea } from "@/components/ui/textarea";
import { categoryMeta } from "@/lib/warehouse";
import { trpc } from "@/lib/trpc";
import { useEffect, useState } from "react";

export type EditablePart = { id: number; partNumber: string; name: string; description: string | null; category: "Medical" | "Embedded" | "Electronics" | "Boards"; warehouseSection: "components" | "products"; componentTypeId: number | null; quantity: number; minimumStock: number; location: string | null };
type PartFormValues = { partNumber: string; name: string; description: string; category: EditablePart["category"]; warehouseSection: EditablePart["warehouseSection"]; componentTypeId: number | null; quantity: number; minimumStock: number; location: string };

export default function PartFormDialog({ open, onOpenChange, part, defaultSection, saving, onSave }: { open: boolean; onOpenChange: (open: boolean) => void; part: EditablePart | null; defaultSection: "components" | "products"; saving: boolean; onSave: (values: PartFormValues) => void }) {
  const { data: componentTypes } = trpc.organization.componentTypes.list.useQuery(); const emptyPart: PartFormValues = { partNumber: "", name: "", description: "", category: "Electronics", warehouseSection: defaultSection, componentTypeId: null, quantity: 0, minimumStock: 0, location: "" }; const [form, setForm] = useState<PartFormValues>(emptyPart);
  useEffect(() => { setForm(part ? { partNumber: part.partNumber, name: part.name, description: part.description ?? "", category: part.category, warehouseSection: part.warehouseSection, componentTypeId: part.componentTypeId, quantity: part.quantity, minimumStock: part.minimumStock, location: part.location ?? "" } : emptyPart); }, [part, open, defaultSection]);
  const setField = <K extends keyof PartFormValues>(key: K, value: PartFormValues[K]) => setForm(current => ({ ...current, [key]: value })); const singular = form.warehouseSection === "products" ? "منتج" : "مكون";
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-2xl overflow-hidden p-0"><DialogHeader className="border-b border-[#E8EEF3] bg-[#F7FBFF] px-6 pb-5 pt-6"><p className="eyebrow">{form.warehouseSection === "products" ? "دليل المنتجات" : "دليل المكونات"}</p><DialogTitle className="text-xl text-[#0B2E4E]">{part ? `تعديل ${singular}` : `إضافة ${singular}`}</DialogTitle><DialogDescription className="text-slate-500">حدّث الكميات والحدود الدنيا باستمرار حتى تبقى تنبيهات المخزون دقيقة.</DialogDescription></DialogHeader><form onSubmit={event => { event.preventDefault(); if (form.partNumber.trim() && form.name.trim()) onSave({ ...form, partNumber: form.partNumber.trim(), name: form.name.trim() }); }} className="space-y-5 p-6"><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label htmlFor="part-number">كود الـ{singular}</Label><Input id="part-number" value={form.partNumber} onChange={event => setField("partNumber", event.target.value)} required /></div><div className="space-y-2"><Label htmlFor="part-name">الاسم</Label><Input id="part-name" value={form.name} onChange={event => setField("name", event.target.value)} required /></div></div><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label>التصنيف الهندسي</Label><Select value={form.category} onValueChange={value => setField("category", value as PartFormValues["category"])}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(categoryMeta).map(([value, meta]) => <SelectItem key={value} value={value}>{meta.arabic}</SelectItem>)}</SelectContent></Select></div><div className="space-y-2"><Label htmlFor="part-location">مكان التخزين</Label><Input id="part-location" value={form.location} onChange={event => setField("location", event.target.value)} placeholder="A-03 / Bin 11" /></div></div>{form.warehouseSection === "components" && <div className="space-y-2"><Label>نوع المكون</Label><Select value={form.componentTypeId ? String(form.componentTypeId) : "untyped"} onValueChange={value => setField("componentTypeId", value === "untyped" ? null : Number(value))}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="untyped">عام / دون نوع</SelectItem>{componentTypes?.filter(type => type.isActive).map(type => <SelectItem key={type.id} value={String(type.id)}>{type.name}</SelectItem>)}</SelectContent></Select><p className="text-xs text-slate-500">استخدم النوع العام للمستلزمات المشتركة مثل الصابون واللاصق ومواد الشركة.</p></div>}<div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label htmlFor="part-quantity">الكمية المتاحة</Label><Input id="part-quantity" type="number" min="0" value={form.quantity} onChange={event => setField("quantity", Math.max(0, Number(event.target.value)))} required /></div><div className="space-y-2"><Label htmlFor="minimum-stock">الحد الأدنى</Label><Input id="minimum-stock" type="number" min="0" value={form.minimumStock} onChange={event => setField("minimumStock", Math.max(0, Number(event.target.value)))} required /></div></div><div className="space-y-2"><Label htmlFor="part-description">الوصف والمواصفات</Label><Textarea id="part-description" value={form.description} onChange={event => setField("description", event.target.value)} rows={4} /></div><DialogFooter><Button type="button" variant="outline" onClick={() => onOpenChange(false)}>إلغاء</Button><Button type="submit" disabled={saving} className="bg-[#0178D4] text-white hover:bg-[#0065B3]">{saving ? "يجري الحفظ…" : part ? "حفظ التعديلات" : `إضافة ${singular}`}</Button></DialogFooter></form></DialogContent></Dialog>;
}
