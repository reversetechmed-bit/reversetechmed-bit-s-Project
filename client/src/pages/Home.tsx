import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Skeleton } from "@/components/ui/skeleton";
import { useAuth } from "@/_core/hooks/useAuth";
import { categoryMeta, formatDate, requestStatusMeta } from "@/lib/warehouse";
import { trpc } from "@/lib/trpc";
import { Activity, ArrowLeft, Boxes, ClipboardCheck, ClipboardList, PackageOpen, TriangleAlert, UsersRound } from "lucide-react";
import { useLocation } from "wouter";

export default function Home() {
  const { user } = useAuth();
  const [, setLocation] = useLocation();
  const isAdmin = user?.role === "admin";
  const { data: dashboard, isLoading: dashboardLoading } = trpc.warehouse.dashboard.useQuery(undefined, { enabled: isAdmin });
  const { data: parts, isLoading: partsLoading } = trpc.warehouse.inventory.list.useQuery();
  const { data: requests, isLoading: requestsLoading } = trpc.warehouse.requests.list.useQuery();
  const activeRequests = (requests ?? []).filter(({ request }) => request.status === "pending" || request.status === "approved");
  return <div className="space-y-6"><section className="relative overflow-hidden rounded-2xl bg-[#0B2E4E] px-6 py-7 text-white sm:px-8 sm:py-9"><div className="absolute inset-0 opacity-40" style={{ backgroundImage: "radial-gradient(circle at 20% 30%, rgba(95,182,242,.5), transparent 24%), radial-gradient(circle at 35% 110%, rgba(0,179,154,.28), transparent 27%), linear-gradient(230deg, transparent 55%, rgba(255,255,255,.07) 55%, transparent 56%)" }} /><div className="relative flex flex-col gap-6 sm:flex-row sm:items-end sm:justify-between"><div><div className="inline-flex rounded-md bg-white px-2 py-1"><img src="/manus-storage/reverse-tech-logo_04d48f19.webp" alt="REVERSE TECH" className="h-6 w-auto" /></div><p className="mt-3 text-[10px] font-extrabold tracking-[0.12em] text-[#5FB6F2]">تحليل · تصميم · تصنيع</p><h1 className="mt-2 text-2xl font-extrabold sm:text-3xl">{isAdmin ? "مركز إدارة المخزون" : `مرحبًا، ${user?.name?.split(" ")[0] || "مهندس"}`}</h1><p className="mt-2 max-w-xl text-sm leading-6 text-[#CDE8FA]">{isAdmin ? "تابع المكونات والمنتجات وطلبات الصرف وصحة المخزون عبر جميع فرق REVERSE TECH." : "ابحث عن المكونات والمنتجات، وقدّم طلب صرف، وتابع كل مراحل التسليم."}</p></div><Button onClick={() => setLocation(isAdmin ? "/requests" : "/inventory")} className="shrink-0 gap-2 bg-[#FF7A29] text-white hover:bg-[#E76415]">{isAdmin ? "مراجعة الطلبات" : "استعراض المخزون"}<ArrowLeft className="h-4 w-4" /></Button></div></section>{isAdmin ? <><AdminOverview dashboard={dashboard} loading={dashboardLoading} requests={requests} requestsLoading={requestsLoading} setLocation={setLocation} /><AdminActivity dashboard={dashboard} loading={dashboardLoading} /></> : <EngineerOverview parts={parts} partsLoading={partsLoading} activeRequests={activeRequests} requestsLoading={requestsLoading} setLocation={setLocation} />}</div>;
}

function AdminOverview({ dashboard, loading, requests, requestsLoading, setLocation }: { dashboard: any; loading: boolean; requests: any; requestsLoading: boolean; setLocation: (to: string) => void }) {
  const cards = [{ label: "المكونات", value: dashboard?.componentCount ?? 0, note: `${dashboard?.componentUnits ?? 0} وحدة بالمخزون`, icon: Boxes, color: "text-[#0178D4] bg-[#E7F3FE]" }, { label: "المنتجات", value: dashboard?.productCount ?? 0, note: `${dashboard?.productUnits ?? 0} وحدة بالمخزون`, icon: PackageOpen, color: "text-[#00A58D] bg-[#E7F8F4]" }, { label: "بانتظار المراجعة", value: dashboard?.pendingRequests ?? 0, note: "تنتظر قرار الأدمن", icon: ClipboardList, color: "text-amber-700 bg-amber-50" }, { label: "مخزون منخفض", value: dashboard?.lowStockParts.length ?? 0, note: "دون الحدود المحددة", icon: TriangleAlert, color: "text-rose-700 bg-rose-50" }];
  const recent = (requests ?? []).slice(0, 4);
  return <><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(card => <div key={card.label} className="panel p-5"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold text-slate-500">{card.label}</p>{loading ? <Skeleton className="mt-3 h-9 w-16" /> : <p className="mt-2 text-3xl font-extrabold text-slate-950">{card.value}</p>}<p className="mt-2 text-xs text-slate-400">{card.note}</p></div><div className={`grid h-10 w-10 place-items-center rounded-xl ${card.color}`}><card.icon className="h-5 w-5" /></div></div></div>)}</section><section className="grid gap-6 xl:grid-cols-[1.4fr_.8fr]"><div className="panel overflow-hidden"><div className="flex items-center justify-between border-b border-slate-100 p-6"><div><p className="eyebrow">قائمة الطلبات</p><h2 className="mt-1 text-lg font-bold text-slate-950">أحدث طلبات المهندسين</h2></div><Button variant="ghost" size="sm" onClick={() => setLocation("/requests")} className="text-slate-600">عرض الكل <ArrowLeft className="mr-1 h-3.5 w-3.5" /></Button></div><div className="divide-y divide-slate-100">{requestsLoading && <div className="p-6"><Skeleton className="h-14 w-full" /></div>}{!requestsLoading && recent.map(({ request, part, engineer }: any) => { const status = requestStatusMeta[request.status]; return <div key={request.id} className="flex items-center gap-4 p-5"><div className="grid h-10 w-10 place-items-center rounded-xl bg-slate-100"><ClipboardCheck className="h-4 w-4 text-slate-600" /></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-slate-900">{part.name} <span className="font-normal text-slate-500">× {request.requestedQuantity}</span></p><p className="mt-1 truncate text-xs text-slate-500">{engineer.name || engineer.email || "مهندس"} · {formatDate(request.createdAt, false)}</p></div><Badge variant="outline" className={part.warehouseSection === "products" ? "border-[#FFD3B7] bg-[#FFF3EB] text-[#C74D00]" : "border-[#BEECDD] bg-[#E7F8F4] text-[#008E7A]"}>{part.warehouseSection === "products" ? "منتج" : "مكون"}</Badge><Badge variant="outline" className={status.className}>{status.label}</Badge></div>; })}{!requestsLoading && !recent.length && <div className="p-10 text-center text-sm text-slate-500">لا توجد طلبات مسجلة حتى الآن.</div>}</div></div><div className="panel overflow-hidden"><div className="border-b border-slate-100 p-6"><p className="eyebrow">حالة المخزون</p><h2 className="mt-1 text-lg font-bold text-slate-950">يحتاج إلى متابعة</h2></div><div className="space-y-3 p-5">{loading && <Skeleton className="h-16 w-full" />}{!loading && dashboard?.lowStockParts.map((part: any) => <div key={part.id} className="rounded-xl border border-rose-100 bg-rose-50/50 p-3.5"><div className="flex items-start gap-3"><TriangleAlert className="mt-0.5 h-4 w-4 text-rose-600" /><div><div className="flex items-center gap-2"><p className="text-sm font-semibold text-rose-950">{part.name}</p><Badge variant="outline" className={part.warehouseSection === "products" ? "border-[#FFD3B7] bg-[#FFF3EB] text-[#C74D00]" : "border-[#BEECDD] bg-[#E7F8F4] text-[#008E7A]"}>{part.warehouseSection === "products" ? "منتج" : "مكون"}</Badge></div><p className="mt-1 text-xs text-rose-700">المتبقي {part.quantity} وحدة · الحد الأدنى {part.minimumStock}</p></div></div></div>)}{!loading && !dashboard?.lowStockParts.length && <div className="py-7 text-center"><div className="mx-auto grid h-10 w-10 place-items-center rounded-xl bg-emerald-50"><ClipboardCheck className="h-5 w-5 text-emerald-600" /></div><p className="mt-3 text-sm font-semibold text-slate-800">مستويات المخزون مستقرة</p><p className="mt-1 text-xs text-slate-500">لا توجد قطعة دون حدها الأدنى.</p></div>}</div></div></section></>;
}

function AdminActivity({ dashboard, loading }: { dashboard: any; loading: boolean }) {
  return <section className="grid gap-6 xl:grid-cols-2"><div className="panel overflow-hidden"><div className="border-b border-[#E8EEF3] p-6"><div className="flex items-center gap-2"><UsersRound className="h-4 w-4 text-[#0178D4]" /><div><p className="eyebrow">آخر دخول</p><h2 className="mt-1 text-lg font-bold text-[#0B2E4E]">آخر مستخدمي المخزن</h2></div></div></div><div className="divide-y divide-[#E8EEF3]">{loading && <div className="p-6"><Skeleton className="h-10 w-full" /></div>}{!loading && dashboard?.recentAccess?.map((person: any) => <div key={person.id} className="flex items-center gap-3 p-5"><div className="grid h-9 w-9 place-items-center rounded-full bg-[#E7F3FE] text-xs font-bold text-[#0178D4]">{person.name?.[0]?.toUpperCase() || person.email?.[0]?.toUpperCase() || "م"}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-[#0B2E4E]">{person.name || person.email}</p><p className="mt-1 text-xs text-slate-500">{person.role === "admin" ? "أدمن" : "مستخدم"} · {formatDate(person.lastSignedIn)}</p></div></div>)}{!loading && !dashboard?.recentAccess?.length && <div className="p-10 text-center text-sm text-slate-500">لا يوجد سجل دخول بعد.</div>}</div></div><div className="panel overflow-hidden"><div className="border-b border-[#E8EEF3] p-6"><div className="flex items-center gap-2"><Activity className="h-4 w-4 text-[#00A58D]" /><div><p className="eyebrow">حركة المخزن</p><h2 className="mt-1 text-lg font-bold text-[#0B2E4E]">أحدث عمليات المخزون</h2></div></div></div><div className="divide-y divide-[#E8EEF3]">{loading && <div className="p-6"><Skeleton className="h-10 w-full" /></div>}{!loading && dashboard?.recentActivities?.map((entry: any) => <div key={entry.activity.id} className="p-5"><div className="flex items-center justify-between gap-3"><p className="text-sm font-semibold text-[#0B2E4E]">{entry.activity.title}</p><span className="whitespace-nowrap text-[11px] text-slate-400">{formatDate(entry.activity.createdAt)}</span></div><p className="mt-1 text-xs text-slate-500">{entry.actor?.name || entry.actor?.email || "النظام"} · {entry.activity.detail}</p></div>)}{!loading && !dashboard?.recentActivities?.length && <div className="p-10 text-center text-sm text-slate-500">ستظهر عمليات المخزن هنا.</div>}</div></div></section>;
}

function EngineerOverview({ parts, partsLoading, activeRequests, requestsLoading, setLocation }: { parts: any; partsLoading: boolean; activeRequests: any[]; requestsLoading: boolean; setLocation: (to: string) => void }) {
  const availableParts = (parts ?? []).filter((part: any) => part.quantity > 0);
  return <><section className="grid gap-4 sm:grid-cols-3"><div className="panel p-5"><p className="text-xs font-semibold text-slate-500">قطع متاحة</p><p className="mt-2 text-3xl font-extrabold text-slate-950">{partsLoading ? "—" : availableParts.length}</p><p className="mt-2 text-xs text-slate-400">جاهزة للطلب</p></div><div className="panel p-5"><p className="text-xs font-semibold text-slate-500">طلبات نشطة</p><p className="mt-2 text-3xl font-extrabold text-slate-950">{requestsLoading ? "—" : activeRequests.length}</p><p className="mt-2 text-xs text-slate-400">قيد الانتظار أو معتمدة</p></div><div className="panel flex flex-col justify-between p-5"><p className="text-xs font-semibold text-slate-500">تحتاج إلى مكون؟</p><Button onClick={() => setLocation("/inventory")} className="mt-4 bg-slate-950 text-white hover:bg-slate-800">استعرض المخزون وقدّم طلبًا</Button></div></section><section className="grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><div className="panel overflow-hidden"><div className="flex items-center justify-between border-b border-slate-100 p-6"><div><p className="eyebrow">دليل القطع</p><h2 className="mt-1 text-lg font-bold text-slate-950">متاح للطلب</h2></div><Button variant="ghost" size="sm" onClick={() => setLocation("/inventory")}>استعرض الدليل <ArrowLeft className="mr-1 h-3.5 w-3.5" /></Button></div><div className="divide-y divide-slate-100">{partsLoading && <div className="p-6"><Skeleton className="h-12 w-full" /></div>}{!partsLoading && availableParts.slice(0, 4).map((part: any) => { const meta = categoryMeta[part.category]; return <div key={part.id} className="flex items-center gap-3 p-5"><div className={`grid h-9 w-9 place-items-center rounded-lg ${meta.soft}`}><PackageOpen className={`h-4 w-4 ${meta.accent}`} /></div><div className="min-w-0 flex-1"><p className="text-sm font-semibold text-slate-900">{part.name}</p><p className="mt-1 text-xs text-slate-500 font-mono">{part.partNumber}</p></div><div className="text-left"><p className="text-sm font-bold text-slate-900">{part.quantity}</p><p className="text-[10px] text-slate-400">وحدة</p></div></div>; })}{!partsLoading && !availableParts.length && <div className="p-10 text-center text-sm text-slate-500">لا توجد قطع متاحة حاليًا.</div>}</div></div><div className="panel overflow-hidden"><div className="border-b border-slate-100 p-6"><p className="eyebrow">طلباتي</p><h2 className="mt-1 text-lg font-bold text-slate-950">الحالة الحالية</h2></div><div className="divide-y divide-slate-100">{requestsLoading && <div className="p-6"><Skeleton className="h-12 w-full" /></div>}{!requestsLoading && activeRequests.map(({ request, part }: any) => <div key={request.id} className="p-5"><div className="flex items-center justify-between gap-2"><p className="truncate text-sm font-semibold text-slate-900">{part.name} × {request.requestedQuantity}</p><Badge variant="outline" className={requestStatusMeta[request.status].className}>{requestStatusMeta[request.status].label}</Badge></div><p className="mt-2 text-xs text-slate-500">قُدّم في {formatDate(request.createdAt, false)}</p></div>)}{!requestsLoading && !activeRequests.length && <div className="p-10 text-center"><p className="text-sm text-slate-500">لا توجد طلبات نشطة.</p><Button variant="outline" size="sm" onClick={() => setLocation("/inventory")} className="mt-3">استعرض المخزون</Button></div>}</div></div></section></>;
}
